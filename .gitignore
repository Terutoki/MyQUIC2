# build artifacts
/target/

# AI agent workspace (local only, not part of the deliverable)
.omo/

# machine-generated identity: server regenerates on first run,
# client must receive server-cert.pem out-of-band. NEVER commit keys.
*.pem

# macOS / editors
.DS_Store
*.swp
*~
.vscode/
.idea/
